# 유스보이스 홈페이지

정적 사이트(HTML/CSS/JS, 빌드 도구 없음). 로컬 미리보기: `python3 -m http.server 8899`.

## 커밋/백업 규칙 (중요)

이 저장소는 노트북 고장 등 만약의 상황에 대비해 GitHub(`https://github.com/youthitdev/youthvoice-homepage`)에 백업하기 위해 만들어졌습니다.

**사용자가 요청한 작업(수정·추가·삭제)을 완료하고 브라우저에서 확인까지 끝나면, 매번 자동으로 git commit + push 하세요.** 사용자에게 "커밋할까요?"라고 다시 묻지 않아도 됩니다 — 이미 승인된 표준 절차입니다.

- 커밋 메시지는 무엇을 왜 바꿨는지 한두 문장으로 간결하게 (한국어로 작성)
- 자잘한 오타 수정이라도 커밋합니다. 다만 같은 대화 턴 안에서 이어지는 여러 개의 작은 수정은 하나의 커밋으로 묶어도 됩니다
- `git push`가 인증 문제로 실패하면(예: 토큰 만료) 사용자에게 알리고, 재인증은 사용자가 직접 하도록 안내하세요 (자격 증명은 절대 대신 입력하지 않습니다)
- `.claude/settings.local.json`은 `.gitignore`에 포함되어 있으니 커밋 대상에서 자동 제외됩니다
